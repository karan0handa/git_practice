// Initializing "#" such that each iteration gets saved.
let hash = "#";
// Creating a for loop such that "#" gets added to the string seven times.
for (i = 1; i <= 7; i++) {
    console.log(hash);
    hash = hash + "#"
}